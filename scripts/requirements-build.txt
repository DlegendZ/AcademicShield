# Build-time only — needed to RETRAIN + TRANSPILE the models to JS.
# Not needed to serve the deployed app (web/api only needs web/requirements.txt).
# Install on top of the root requirements.txt:
#   pip install -r requirements.txt -r scripts/requirements-build.txt
m2cgen==0.10.0
imbalanced-learn==0.14.1
